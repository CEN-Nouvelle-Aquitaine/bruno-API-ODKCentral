meta {
  name: LogIn
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/sessions
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

vars:post-response {
  token: res.body.token
}
