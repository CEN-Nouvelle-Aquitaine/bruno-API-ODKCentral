meta {
  name: Datasets list
  type: http
  seq: 4
}

get {
  url: {{base_url}}/v1/projects/{{projectid}}/datasets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  projectid: 
}

docs {
  Listing all datasets linked to the specified project.
  
  
}
